spring:
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yaml
  application:
    name: admin-server
  profiles:
    active: dev
  #读取配置文件的dataID : admin-server-dev.yaml